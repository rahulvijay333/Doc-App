

import 'package:flutter/material.dart';

class ScreenAvailableTime extends StatelessWidget {
  const ScreenAvailableTime({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Available'),
      ),
    );
  }
}
